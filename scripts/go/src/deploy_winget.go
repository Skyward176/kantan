// Will probably be the hardest. Has to build msi/exe, then generate manifest, 
// then make a github release for the installer, then submit the manifest.
// Will likely end up requiring a docker container or something to use the windows specific tools 
// that are required here. Or some other solution to that.  
